apiVersion: apps/v1
kind: Deployment
metadata:
  name: playground-server-http-canary
  namespace: {{ .Values.namespace.name }}
  labels:
    app: playground-server-http
    role: canary
    app.kubernetes.io/name: playground-server-http
    app.kubernetes.io/part-of: playground
spec:
  replicas: {{ .Values.http.canary.replicas }}
  selector:
    matchLabels:
      app: playground-server-http
      role: canary
  template:
    metadata:
      labels:
        app: playground-server-http
        role: canary
        app.kubernetes.io/name: playground-server-http
        app.kubernetes.io/part-of: playground
      {{- with .Values.http.canary.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ .Values.http.canary.serviceAccount.name }}
      containers:
        - name: server
          image: "{{ .Values.http.image.repository }}:{{ .Values.http.image.tag }}"
          imagePullPolicy: {{ .Values.http.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
          env:
            {{- range $k, $v := .Values.http.canary.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
            failureThreshold: 5
          resources:
            {{- toYaml .Values.http.canary.resources | nindent 12 }}
